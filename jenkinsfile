pipeline {
    agent any
    stages {
        stage('Setup Python') {
            steps {
                sh 'python3 --version'  // Or use python specific to your setup
            }
        }
    }
}